// Funciones: son bloques de codigo o subproceso que estan aislados
//nos permite reutilizarlo las veces que se quiera

//parametros: variable que se especifica en los parentecis de la funcion
//argumentos: dato que se le pone al parametro

/* function saludar(nombre) {
  //nombre seria el parametro
  console.log("hola " + nombre);
}
saludar("Deissy"); //Deissy seria el argumento
 */

/* function calculadora(numero1, numero2) {
  var suma = numero1 + numero2;
  return suma;
}
console.log(calculadora(3, 4));
 */

/* function mostarPantalla(n1, n2) {
  document.write(n1 + n2);
}
function monstrarConsola(n1, n2) {
  console.log(n1 + n2);
}
function calculadora(numero1, numero2, mostrar = true) {
  if (mostrar == true) {
    mostarPantalla(numero1, numero2);
  } else {
    monstrarConsola(numero1, numero2);
  }
}
calculadora(3, 4); */

/* function suma(numero1, numero2) {
  var resultado;
  resultado = numero1 + numero2;
  return resultado;
}

var sumatoria;
console.log(suma(4, 8)); */
